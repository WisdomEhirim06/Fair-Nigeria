/**
 * Notifications module (Sprint 5): dispatches Firebase Cloud Messaging (FCM) push
 * notifications - chiefly transcription task assignments. It is called directly by
 * other modules (e.g. consensus) and exposes no HTTP routes, which is why this
 * module has a service file instead of a router.
 */

export {};
