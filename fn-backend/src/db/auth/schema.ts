/**
 * Auth database schema (fn_auth).
 *
 * Tables are added in Sprint 2 (T1.5):
 *   users         — registered citizen accounts + hashed NIN
 *   otp_records   — one-time passwords for phone verification
 *   refresh_tokens — JWT refresh token store
 *
 * This file is the single entry point for drizzle.auth.config.ts.
 * Export every table object from here so drizzle-kit picks them all up.
 */

// No tables yet — added in T1.5.
export {};
