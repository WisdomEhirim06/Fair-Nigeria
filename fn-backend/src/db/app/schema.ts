/**
 * Main application database schema (fn_app).
 *
 * Tables are added progressively across sprints:
 *   Sprint 3 — elections, articles, geography (states, lgas)
 *   Sprint 4 — ratings
 *   Sprint 5 — uploads, sheets, consensus
 *   Sprint 3 — audit_log
 *
 * This file is the single entry point for drizzle.app.config.ts.
 * Export every table object from here so drizzle-kit picks them all up.
 */

// No tables yet — added in Sprint 3+.
export {};
